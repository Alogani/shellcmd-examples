#[
  version       = "0.1.0"
  author        = "Anonymous"
  description   = "What your package do"
  license       = "MIT"

  requires "shellcmd = 0.1.0"
]#

#[ The following fields are mandatory :
  * version
  * description
  * requires with shellcmd version used
  * requires for external dependencies
]#
